# Serializers completos de apps.monitoring

Copiar la carpeta `apps/monitoring/serializers/` al backend y ejecutar:

```cmd
.\.venv\Scripts\python.exe manage.py check
```

Incluye serializers para los 38 modelos, registro y heartbeat del agente, token de instalación, credenciales SNMP protegidas y payload de ingesta de capturas.
